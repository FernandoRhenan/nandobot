POSTGRES_HOST=localhost
POSTGRES_USER=local_user
POSTGRES_PASSWORD=local_password
POSTGRES_DB=radar_de_ofertas
POSTGRES_PORT=5432
POSTGRES_CA=
DATABASE_URL=postgres://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:$POSTGRES_PORT/$POSTGRES_DB?sslmode=disable

# Redis

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_URL=redis://localhost:6379

# Socket.IO

SOCKET_PORT=3010
SOCKET_CORS_ORIGIN=http://localhost:3000
SOCKET_INTERNAL_URL=http://localhost:3010/internal
NEXT_PUBLIC_SOCKET_URL=http://localhost:3010

# Auth

AUTH_PASSWORD=troque-esta-senha
AUTH_SECRET=troque-por-um-segredo-longo-e-aleatorio

# Tests

TEST_AFFILIATE_URL_MELI=
